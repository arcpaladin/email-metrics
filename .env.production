# Production Environment Variables for AWS Deployment
# These values should be set in AWS App Runner environment variables

# Database Configuration (Amazon RDS PostgreSQL)
DATABASE_URL=postgresql://dbadmin:${DB_PASSWORD}@email-analytics-db.cluster-xxxxxxxxx.us-east-1.rds.amazonaws.com:5432/emailanalytics
PGHOST=email-analytics-db.cluster-xxxxxxxxx.us-east-1.rds.amazonaws.com
PGPORT=5432
PGDATABASE=emailanalytics
PGUSER=dbadmin
PGPASSWORD=${DB_PASSWORD}

# OpenAI Configuration
OPENAI_API_KEY=${OPENAI_API_KEY}

# Microsoft Azure Configuration
VITE_AZURE_CLIENT_ID=${AZURE_CLIENT_ID}
VITE_AZURE_TENANT_ID=${AZURE_TENANT_ID}
VITE_AZURE_AUTHORITY=https://login.microsoftonline.com/${AZURE_TENANT_ID}
VITE_REDIRECT_URI=https://your-app-domain.amplifyapp.com

# JWT Configuration
JWT_SECRET=${JWT_SECRET}

# Application Configuration
NODE_ENV=production
PORT=5000

# AWS Configuration
AWS_REGION=us-east-1